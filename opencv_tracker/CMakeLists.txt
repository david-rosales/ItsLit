cmake_minimum_required(VERSION 2.8)
project( its_lit )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} ${C_SERIAL_INCLUDE_DIRECTORIES})
add_executable( its_lit its_lit.cpp )
target_link_libraries( its_lit cserial ${OpenCV_LIBS} )
